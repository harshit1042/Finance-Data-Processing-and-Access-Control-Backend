package com.assessment.finance.dto; import com.assessment.finance.model.RecordType; import jakarta.validation.constraints.*; import java.math.BigDecimal; import java.time.LocalDate; public record CreateRecordRequest(@NotNull(message="amount is required") @DecimalMin(value="0.01",message="amount must be greater than zero") BigDecimal amount,@NotNull(message="type is required") RecordType type,@NotBlank(message="category is required") String category,@NotNull(message="date is required") LocalDate date,@Size(max=500,message="notes must be <= 500 chars") String notes){}
